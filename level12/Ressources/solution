В нашей директории есть файл level12.pl.
При просмотре которого понятно что на localhost на порту 4646 что-то есть.
Так же понятно, что функция n ждет два параметра x и y. 
curl -v localhost:4646?x=\`getflag\`		На этот раз не работает, как это было на level04.

Так же в скрипте исполуется функция еgrep, которая работает как echo, вызывает команду в ``. Но так же используется команда tr, которая заменяет маленькие буквы полученные в качестве параметра х на большие.
В переменную хх должна попасть команда getflag написанная маленькими буквами, так как unix ригистрзависимая система. Для этого создаем файл, имя которого будет изначально прописано прописными буквами, в котором и будет наша команда.
Название директории tmp можем написать *.

Создаем файл в директории /tmp FLAG.
Затем
chmod a+x /tmp/FLAG
Отправляем на сервер путь к файлу
> curl -v localhost:4646?x="\`/*/FLAG\`"

И смотрим результат:
> cat result
Check flag.Here is your token : g1qKMiRpXf53AWhDaU7FEkczr
